# Data Directory

This directory is reserved for runtime and seed data.

- `postgres/` is mounted by Docker Compose for local database persistence.
- additional import artifacts can live here later if the seed workflow needs them.
